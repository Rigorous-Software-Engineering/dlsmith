import fp
import intern
import souffle_lib
import souffle_types
typedef TR = Option<(Tfloat, Tunsigned, Tnumber, Tsymbol)>
output relation RRel(_r:TR)
RRel(Some{((- (64'f1.5: Tfloat)), (4294967295), (- (5)), string_intern("meow"))}).
RRel(Some{((1), (1), (1), string_intern("1"))}).
