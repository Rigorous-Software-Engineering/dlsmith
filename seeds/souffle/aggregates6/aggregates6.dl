
.decl A(x:number) 
.decl B(x:number) 

A(-2147483648). // INT_MIN 32 bits
B(2147483647).  // INT_MAX 32 bits.


.decl C(x: number) 
.decl D(x: number) 
.output C,D

C(x) :- x = max y : A(y).
D(x) :- x = min y : B(y).
